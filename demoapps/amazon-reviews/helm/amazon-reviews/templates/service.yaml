apiVersion: v1
kind: Service
metadata:
  name: {{ include "amazon-reviews.fullname" . }}
  labels:
    {{- include "amazon-reviews.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amazon-reviews.selectorLabels" . | nindent 4 }}
